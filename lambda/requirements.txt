# Minimal, pinned requirements for the Lambda container (POC)
# - Pinned to match project requirements.txt where relevant
# - Avoids dev/notebook deps to keep the image smaller

# Core runtime
pandas==2.3.1
joblib==1.5.2
boto3==1.40.40
numpy==2.3.2
scikit-learn==1.7.2  # required to load the saved scaler via joblib

# GRIB reading stack (for GEFS)
xarray==2025.9.0
cfgrib==0.9.15.0
eccodes==2.43.0
eccodeslib==2.43.0
eckitlib==1.31.4
fckitlib==0.14.0
h5py==3.14.0
h5netcdf==1.6.4
cftime==1.6.4

# TensorFlow inference
tensorflow==2.20.0
tensorflow-probability==0.25.0
keras==3.11.3
tf-keras==2.20.1

# Project package (editable install from repo root parent)
-e ..
